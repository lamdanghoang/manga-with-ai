// Deprecated — replaced by PackageModal
// Keeping as empty export for backward compatibility
export function VipModal(_props: { isOpen: boolean; onClose: () => void; onSuccess: () => void }) {
  return null;
}
